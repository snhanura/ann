# include<stdio.h>
void main()
{
int a[100],n;
printf("enter the n value");
scanf("%d",&n);
for(i=0;a[i]<n;i++)
{
scanf("%d",&a[i]);
}
for(i=0;i<a[i];i++)
{
if(a[i]>a[0])
{
hi=a[i];
}
printf("the highest value is%d",a[i]);
if(a[i]<a[0])
{
low=a[i];
}
printf(" the lowest value is%d",a[i]);
}
